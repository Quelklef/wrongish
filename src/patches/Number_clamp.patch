::host Number
::type (this: number | Number, lo: number, hi: number) => number
::syms clamp

::docn
Return the nearest number in the range `[lo, hi]`.

::impl
function(lo, hi) {
  return Math.max(lo, Math.min(hi, +this));
}

::test

expect(M.clamp(9, 1, 5)).toBe(5);
expect(new Number(9)[W.clamp](1, 5)).toBe(5);

expect(M.clamp(3, 1, 5)).toBe(3);
expect(new Number(3)[W.clamp](1, 5)).toBe(3);

expect(M.clamp(0, 1, 5)).toBe(1);
expect(new Number(0)[W.clamp](1, 5)).toBe(1);

