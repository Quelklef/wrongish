::host Map<K, V>
::type (key: K, defaultValue: V) => V
::syms getOrCreate

::docn
Get a value from a map, or create a new one if it doesn't exist.

::impl
function(key, deflt) {
  if (this.has(key)) {
    return this.get(key);
  } else {
    this.set(key, deflt);
    return deflt;
  }
}

::test
const m = new Map();
expect(m[W.getOrCreate]('x', 'y')).toBe('y');
expect(m[W.getOrCreate]('x', 'z')).toBe('y');
