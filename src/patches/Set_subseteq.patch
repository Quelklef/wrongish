::host Set<T>
::type (other: Set<T>) => boolean
::syms subsetEq subseteq le leq

::docn
Is this set `<=` another set?

::impl
function(other) {
  if (this.size > other.size)
    return false;

  for (const item of this)
    if (!other.has(item))
      return false

  return true;
}

::test
const all = new Set([-2, -1, 0, 1, 2]);
const mid = new Set([-1, 0, 1]);
const neg = new Set([-2, -1]);
const pos = new Set([1, 2]);

expect(all[$.subseteq](all)).toBe(true);
expect(mid[$.subseteq](all)).toBe(true);
expect(neg[$.subseteq](all)).toBe(true);
expect(pos[$.subseteq](all)).toBe(true);

expect(mid[$.subseteq](neg)).toBe(false);
expect(all[$.subseteq](mid)).toBe(false);
expect(pos[$.subseteq](mid)).toBe(false);
