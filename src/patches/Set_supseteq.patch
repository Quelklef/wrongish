::host Set
::hvar <T>
::type (other: Set<T>): boolean
::syms supersetEq supsetEq supseteq ge geq

::docn
Is this set `>=` another set?

::impl
function(other) {
  if (this.size < other.size)
    return false;

  for (const item of other)
    if (!this.has(item))
      return false

  return true;
}

::test
const all = new Set([-2, -1, 0, 1, 2]);
const mid = new Set([-1, 0, 1]);
const neg = new Set([-2, -1]);
const pos = new Set([1, 2]);

expect(all[$.supseteq](all)).toBe(true);
expect(all[$.supseteq](mid)).toBe(true);
expect(all[$.supseteq](neg)).toBe(true);
expect(all[$.supseteq](pos)).toBe(true);

expect(neg[$.supseteq](mid)).toBe(false);
expect(mid[$.supseteq](all)).toBe(false);
expect(mid[$.supseteq](pos)).toBe(false);
